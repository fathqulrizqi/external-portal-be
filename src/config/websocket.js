export const EVENTS = {
  CLIENT: {
    JOIN_ROOM: 'client:join-room',
    SEND_MESSAGE: 'client:send-message'
  },
  SERVER: {
    RECEIVE_MESSAGE: 'server:receive-message',
    NOTIFY_JOIN: 'server:notify-join'
  }
};